services:
  monitor:
    build: .
    command: ["python", "-m", "yc_launch_monitor", "serve", "--send-slack"]
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
